/**
 * @file index.ts
 * @description Ponto de entrada do app noMEI.
 * Registra o componente raiz no runtime do Expo/React Native.
 */

import { registerRootComponent } from 'expo';
import App from './App';

registerRootComponent(App);
